{
  "name": "@moltstream/container",
  "version": "0.1.0",
  "description": "Container isolation runtime — each agent stream runs in a sandboxed Docker environment",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@moltstream/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
